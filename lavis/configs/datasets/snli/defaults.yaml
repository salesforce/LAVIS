datasets:
  snli_ve:
    # data_dir: ${env.data_dir}/datasets
    data_type: images # [images|videos|features]
    
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train: 
          url: /export/share/junnan-li/ALBEF/data/ve_train.json
          storage: snli/annotations/ve_train.json
        val:
          url: /export/share/junnan-li/ALBEF/data/ve_dev.json
          storage: snli/annotations/ve_dev.json
        test: 
          url: /export/share/junnan-li/ALBEF/data/ve_test.json
          storage: snli/annotations/ve_test.json
      class_list:
        storage: 
      images:
        train: 
          storage: /export/share/datasets/vision/flickr30k/flickr30k-images
        val: 
          storage: /export/share/datasets/vision/flickr30k/flickr30k-images
        test: 
          storage: /export/share/datasets/vision/flickr30k/flickr30k-images
